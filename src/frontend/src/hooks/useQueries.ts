// This file is intentionally minimal as the calculator does not require backend queries.
// All state is managed locally in the browser.

export function useQueries() {
  // No backend queries needed for this calculator app
  return {};
}
